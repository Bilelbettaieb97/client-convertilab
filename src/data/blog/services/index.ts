import type { FullBlogArticle } from "@/data/blog-articles";

/**
 * Articles de fond liés aux pages de service (un par pôle et par sous-page),
 * écrits en septembre 2026. Un fichier par article dans ce dossier ; ils sont
 * fusionnés en tête de `blogArticles` (les plus récents d'abord).
 */
export const articlesServices: FullBlogArticle[] = [];
